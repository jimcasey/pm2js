{
  "name": "@pm2js/monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "pnpm --stream -r run build",
    "dev": "pnpm --filter=@pm2js/converter run dev",
    "playground": "pnpm --filter=@pm2js/playground run start"
  },
  "devDependencies": {
    "@types/node": "^18.15.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
